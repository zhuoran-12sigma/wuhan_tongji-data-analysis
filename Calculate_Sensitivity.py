# import jsonScan
# import sigmaLU_detection
import os
import pandas as pd
"""
Each folder contains the json files of all patients in one scan
"""
if __name__ == "__main__":
    gt_path = ""    # ground truth path
    dt_paths = ""    # a list detection path, contains 12 different paths
    # traverse all files get TP and FP
    for dt_path in dt_paths:


    """
    evaluate sensitivity, write to the csv files
    """


    """
    evaluate consistency, write to the csv files
    """
